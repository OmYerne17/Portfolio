module.exports = {
    extends: ['next/core-web-vitals'],
    rules: {
      // Add any specific rules you need
    },
    // Add parser options if needed
    parserOptions: {
      ecmaVersion: 2020,
      sourceType: 'module',
      ecmaFeatures: {
        jsx: true,
      },
    },
  }